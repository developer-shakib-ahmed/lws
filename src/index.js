import React from 'react';
import ReactDOM from 'react-dom';

const element1 = (
  <h1 className="text" tabIndex="id-1">
    Hello World
  </h1>
);

ReactDOM.render(element1, document.getElementById('root'));
